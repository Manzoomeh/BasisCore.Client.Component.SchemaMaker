export default interface IDatepickerOptions {
  todayButton: boolean;
  yearsList: boolean;
  monthList: boolean;
  rangeDates: boolean;
  switchType: boolean;
  style: string;
  clockType: "24h" | "12h";
}
